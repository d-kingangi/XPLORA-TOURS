export interface bookings{
    userId: string;
    tourId: string;
    bookingDate: string,
}

export interface updatedbooking{
    bookingId: string;
    userId: string;
    tourId: string;
    bookingDate: string,
}